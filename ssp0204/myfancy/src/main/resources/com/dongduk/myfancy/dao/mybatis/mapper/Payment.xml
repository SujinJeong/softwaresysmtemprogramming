<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dongduk.myfancy.dao.mybatis.mapper.PaymentMapper">

  <insert id="insertPayment" parameterType="com.dongduk.myfancy.domain.Payment">
  	INSERT INTO PAYMENT
  		(SALE_ID,PAYMENT_CODE,AMOUNT)
  	VALUES
  		(#{param1},#{param2},#{param3})
  </insert>
  
  <select id="getPayment" resultType="com.dongduk.myfancy.domain.Payment">
  	SELECT P.SALE_ID,P.PAYMENT_CODE,AMOUNT
  	FROM PAYMENT P,PAYMENT_CODE PC,SALE S
  	WHERE S.SALE_ID=P.SALE_ID AND EMPLOYEE.EMP_ID = #{param1}
  </select>
  
  <select id="getEmployeeList" resultType="com.dongduk.myfancy.domain.Employee">
  	SELECT EMPLOYEE.EMP_ID,
  		STORE.STORE_ID,
  		EMPLOYEE.EMP_NAME,
  		EMPLOYEE.ADDRESS,
  		EMPLOYEE.PHONENUM,
  		EMPLOYEE.EMAIL,
  		EMPLOYEE.WORKTIME,
  		EMPLOYEE.BANKACCOUNT,
  		EMPLOYEE.SALARY
  	FROM EMPLOYEE, STORE
  	WHERE STORE.STORE_ID = #{param1}
  	ORDER BY EMPLOYEE.EMP_ID
  </select>

</mapper> 